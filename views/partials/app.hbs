  <script>
    // Allow easy access to JSON.stringify()'d data.
    {{#each json}}
      define('{{@key}}', function (require) {
        return {{{this}}};
      });
    {{/each}}

    // Load config variables
    define('conf', function(require) {
      return {{{stringify conf}}};
    });

    // Start the app.
    require(['apps/clipr'], function (app) {
      app.start();
    });
  </script>
